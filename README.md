# work-queue-modal-refactor

Sandbox for refactoring the `workQueueModals.js` file from VBMS Core.

This repo contains:
- `src/workQueueModals.js`: a direct copy of the original file, used as a starting point
- Future modularized versions and experiments

No other VBMS Core code is included; only this JS slice is here for refactor and design work.
